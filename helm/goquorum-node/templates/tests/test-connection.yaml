apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "goquorum-node.fullname" . }}-test-connection"
  labels:
    {{- include "goquorum-node.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "goquorum-node.fullname" . }}:80']
  restartPolicy: Never
